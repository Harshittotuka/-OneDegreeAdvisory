{
    "$schema": "https://www.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "vite build",
        "dev": "vite",
        "minify": "esbuild public/styles.css public/stripe-nav.css --minify --outdir=public --entry-names=[name].min && esbuild public/script.js public/stripe-nav.js --minify-whitespace --minify-syntax --outdir=public --entry-names=[name].min"
    },
    "devDependencies": {
        "@tailwindcss/vite": "^4.0.0",
        "concurrently": "^9.0.1",
        "esbuild": "^0.28.2",
        "laravel-vite-plugin": "^3.1",
        "tailwindcss": "^4.0.0",
        "vite": "^8.0.0"
    }
}
